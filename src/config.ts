const config = {
    cellSize: 2,
    cellColumns: 1000 as number | "auto",
    cellRows: 1000 as number | "auto",
    backgroundColor: "#EEEEEE",
    cellColor: "black",
    gridColor: "#FFFFFF",
    frameRate: 12,
    autoRunOnMouseOut: false,
    dragKey: "Space",
    allowSmallerScale: false,
    minGridSize: 8,
    minSelectionBoxSize: 12,
    soupDensity: 0.1
}

export default config;